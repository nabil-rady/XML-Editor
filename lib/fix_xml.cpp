#include "fix_xml.hpp"

QString fix_xml(QString xml){
//    if (check(xml)){
//        return xml;
//    }

    QString ans = "";
    long long len = xml.length();
    for (long long i = 0; i < len; i++){

    }
    return ans;
}
